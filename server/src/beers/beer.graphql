type Beer {
  id: Int!
  tap_number: String
  name: String
  style: String
  brewery: String
  abv: String
  ibu: String
  label_image_hd: String
  position: Int
  price: String
}

type Query {
  beer(id:Int!) : Beer
  beers: [Beer]
}